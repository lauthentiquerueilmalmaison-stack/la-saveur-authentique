import MenuCard from "./MenuCard";
import Section from "./Section";
async function Menu() {
  return (
    <Section id="menu" title="Menu">
      <MenuCard />
    </Section>
  );
}

export default Menu;
